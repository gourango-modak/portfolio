import { primaryTools } from "./../toolGroups/primaryTools";
import { pageTools } from "./../toolGroups/pageTools";
import { otherTools } from "./../toolGroups/otherTools";

export const toolbarConfig = [...primaryTools, pageTools, otherTools];
