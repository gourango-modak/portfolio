const SkillTag = ({ skill }) => (
    <span className="bg-indigo-100 text-indigo-800 py-2 px-4 rounded-full text-sm font-medium inline-block">
        {skill}
    </span>
);

export default SkillTag;
